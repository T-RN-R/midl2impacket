import random
rand = random # so we can change the random implementation...
ITERATION_COUNT = 10 #Number of method calls to execute
USE_EXISTING = 95 # percent chance to use existing variables rather than generate anew
NULL_TERMINATE_CHANCE = 75 # chance to null terminate a cstring

creds = ("m", "password")
hostname = "localhost"